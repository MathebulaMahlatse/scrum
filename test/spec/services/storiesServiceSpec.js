/*
fdescribe('Unit Test for StoriesService', function() {
    beforeEach(module('prodAngularApp.storiesService'));

    var storiesService, httpBackEnd;
    beforeEach(inject(function(StoriesService, $httpBackend) {
        storiesService = StoriesService;
        httpBackEnd = $httpBackend;

        httpBackEnd.
    }));
});*/
